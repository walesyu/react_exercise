language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
  - ls -l
  - pwd
before_deploy:
  - cd $TRAVIS_BUILD_DIR/build
  - ls -l
deploy:
  provider: pages
  cleanup: false
  token: $GITHUB_TOKEN
  verbose: true
  edge: true
  on:
    branch: master
